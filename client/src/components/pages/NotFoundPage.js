import React from 'react'

const NotFoundPage = () => {
    return (
        <div className="NotFoundPage">
            The page requested found not be found. Check the URL again
        </div>
    );
}

export default NotFoundPage;